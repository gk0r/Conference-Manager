<%= form_for(@booking) do |f| %>
  <% if @booking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@booking.errors.count, "error") %> prohibited this booking from being saved:</h2>
      <ul>
      <% @booking.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form">
	<div class="left">
		<%= f.label :date, :class => "edit_label" %> <br />
		<%= f.label :time_start, :class => "edit_label" %> <br />
		<%= f.label :time_finish, :class => "edit_label" %><br />
		
		<% if @ready_to_book %>
			<%= f.label :conference_number_id, :class => "edit_label" %> <br />
		<% end %>
	</div>
	
	<div class="right">
		<%= text_field_tag :date, params[:date], :size => 25, :class => "edit_field" %> <br />
    						
    	<%= text_field_tag :time_start, params[:time_start], :size => 10, :class => "edit_field" %> <br />
							
    	<%= text_field_tag :time_finish, params[:time_finish], :size => 10, :class => "edit_field" %> <br /> 

		<% if @ready_to_book %>
			<%= f.collection_select :conference_number_id, @conference_numbers, :id, :conference_number, 
			{:prompt => true}, {:class => "edit_field"} %> <br />
			
			<%= add_button(button_label, "add.png", "positive edit_field") %> 
		<% else %>		
 			<%= add_button("Check Availability", "zoom.png", "positive edit_field") %>
		<% end %>
					
		<%= add_link("Cancel", "cross.png", "negative edit_field", bookings_path)%>
	</div>
</div>
  
<% end %>

<br /><br /><br /><br /><br /><br /><br /><br /><br /><br />

<%= debug(@booking) %>
<%= debug(params) %>